# 生成测试结果和输出文件

## 🎯 目标

生成可展示的测试结果，证明系统功能完整且正常工作。

---

## 方法一：快速生成演示结果（推荐）

### 运行自动演示脚本

```powershell
python scripts/generate_demo_results.py
```

**这个脚本会：**
1. ✅ 检查模型是否存在
2. ✅ 查找可用的 MOT20 序列
3. ✅ 处理一个序列并生成结果
4. ✅ 创建输出视频、JSON 结果和报告

**生成的文件：**
- `data/processed/demo_results/MOT20-01_demo_[时间戳].mp4` - 带跟踪的可视化视频
- `data/processed/demo_results/MOT20-01_results_[时间戳].json` - 详细统计结果
- `data/processed/demo_results/demo_report_[时间戳].md` - Markdown 报告

---

## 方法二：完整测试套件

### 运行完整测试

```powershell
python scripts/run_complete_test.py
```

**这个脚本会：**
1. ✅ 检查环境配置
2. ✅ 验证模型可用性
3. ✅ 检查数据集
4. ✅ 处理测试序列
5. ✅ 生成测试报告

**输出位置：** `data/processed/test_results/`

---

## 方法三：手动生成结果

### 步骤 1：处理 MOT20 序列并保存结果

```powershell
# 处理序列并保存视频
python -m src.inference.main --input "data/raw/MOT20/MOT20/train/MOT20-01/img1" --output "data/processed/MOT20-01_tracked.mp4" --model models/checkpoints/yolov8n.pt
```

**这会生成：**
- 输出视频：`data/processed/MOT20-01_tracked.mp4`
- 结果 JSON：`data/processed/MOT20-01_tracked.json`

### 步骤 2：查看结果

```powershell
# 查看生成的视频
# 用任何视频播放器打开
data/processed/MOT20-01_tracked.mp4

# 查看统计结果
Get-Content data/processed/MOT20-01_tracked.json | ConvertFrom-Json
```

### 步骤 3：处理多个序列（可选）

```powershell
# 处理更多序列以展示系统能力
python -m src.inference.main --input "data/raw/MOT20/MOT20/train/MOT20-02/img1" --output "data/processed/MOT20-02_tracked.mp4" --model models/checkpoints/yolov8n.pt

python -m src.inference.main --input "data/raw/MOT20/MOT20/test/MOT20-04/img1" --output "data/processed/MOT20-04_tracked.mp4" --model models/checkpoints/yolov8n.pt
```

---

## 📊 结果文件说明

### 1. 输出视频文件 (.mp4)

**内容：**
- 原始视频帧
- 人员检测边界框（绿色）
- 跟踪 ID 标签
- 实时统计信息（帧数、检测数、跟踪数）

**用途：** 直观展示系统检测和跟踪能力

### 2. 结果 JSON 文件 (.json)

**包含：**
```json
{
  "input": "输入路径",
  "output": "输出路径",
  "timestamp": "处理时间",
  "total_frames": 总帧数,
  "total_detections": 总检测数,
  "max_tracks": 最大跟踪数,
  "fps": 帧率,
  "resolution": "分辨率",
  "detection_history": [每帧的检测历史]
}
```

**用途：** 定量分析系统性能

### 3. 测试报告 (.txt 或 .md)

**包含：**
- 环境检查结果
- 模型验证
- 数据集检查
- 处理结果
- 生成文件列表

**用途：** 完整的测试文档

---

## 🎬 展示建议

### 展示内容清单

1. **视频输出**
   - 打开生成的 MP4 文件
   - 展示检测框和跟踪 ID
   - 说明实时统计信息

2. **统计数据**
   - 显示 JSON 文件中的关键指标
   - 总帧数、检测数、跟踪数

3. **系统架构**
   - API 服务器（http://127.0.0.1:8000/docs）
   - Web UI（http://127.0.0.1:8501）
   - 命令行工具

4. **代码结构**
   - 展示项目文件组织
   - 说明主要模块

---

## 📁 输出文件位置

所有生成的文件保存在：

```
data/processed/
├── demo_results/          # 演示结果
│   ├── MOT20-01_demo_*.mp4
│   ├── MOT20-01_results_*.json
│   └── demo_report_*.md
└── test_results/          # 测试结果
    ├── test_report_*.txt
    └── ...
```

---

## ⚡ 快速命令参考

```powershell
# 1. 快速生成演示结果
python scripts/generate_demo_results.py

# 2. 运行完整测试
python scripts/run_complete_test.py

# 3. 手动处理单个序列
python -m src.inference.main --input "data/raw/MOT20/MOT20/train/MOT20-01/img1" --output "data/processed/demo.mp4" --model models/checkpoints/yolov8n.pt

# 4. 查看结果目录
Get-ChildItem data/processed/ -Recurse
```

---

## ✅ 验证清单

生成结果后，检查：

- [ ] 输出视频文件存在且可播放
- [ ] 视频中可以看到检测框和跟踪 ID
- [ ] JSON 文件包含统计数据
- [ ] 测试报告已生成
- [ ] 所有文件大小合理（不为 0）

---

## 🎯 下一步

生成结果后，您可以：

1. **展示视频**：播放生成的 MP4 文件
2. **分析数据**：查看 JSON 中的统计信息
3. **准备演示**：使用报告文件准备演示材料
4. **截图保存**：从视频中截取关键帧作为展示图片

现在运行 `python scripts/generate_demo_results.py` 开始生成结果！


